package expo.modules.onetakemediaresearch

import androidx.core.content.FileProvider

/** Keeps the research provider distinct from the production export provider. */
class ResearchFileProvider : FileProvider()
