# T1.5 acceptance coordination

This is the #62 coordination report for A's #65 persistence implementation.
It is not completed V1, physical-device acceptance or named human sign-off.
Branch: `feat/t15-durable-foundation`, based on merged main `3da03c4`, including PR #58.
Ownership: A persistence/lifecycle, B timeline/editor (#67), C captions/export (#68), original capture and speech owners retain their producer files.
No B/C or capture-owned implementation files were edited by A.

## Implemented acceptance matrix

| Acceptance | Implemented evidence | Integration or acceptance still pending |
| --- | --- | --- |
| Stable project/source/script/span/point/take/clip/job identities and revisions | `t15-schema.ts`, executable `tests/fixtures/t15-foundation.json`, schema migration and source-isolation tests | Producer-owned span/point/take mapping and capture IDs must be wired after #70/#71 merge. |
| Immutable originals and compatible Project migration | Existing Project schema 2 plus optional v15, original alias mapping, legacy trim/transcript/pickups retained, future schema write rejection | Actual old-device databases and corrupt-media UX reproduction. |
| Separate observations, proposals, creator decisions, transcript revisions, corrections/settings | Typed snapshot plus complete paged evidence rows in existing `onetake.db`; no result applies a timeline | Full producer sinks, point controls and both-mode results after merge. |
| Source-relative seconds, provenance, honest uncertainty | Explicit framing-ms adapter, null uncertainty/duration, source bounds and unknown statuses in fixtures | Capture clock/PTS calibration, measured approximately 1 Hz cadence and processor evidence. |
| Timeline history and original preservation | Canonical clips including exclusions, active/abandoned history and individually archived commands, atomic full-vector commit | B authors command semantics and editor integration; physical undo/redo/reopen/export path pending. |
| Independent caption preferences and correction history | `showInEditor`/`burnIntoExport` default true/true; atomic caption adapter and recognition-preserving legacy bridge | C's four preview/export combinations, cue alignment and output inspection. |
| Original durable before optional finalization | Root `saveProjectMetadata` completes existing media journal before returning to caption cleanup; pickup journal retained | Capture lifecycle status/sink wiring and device Stop timing. |
| Checkpoints/jobs/retry/duplicate payloads | Bounded evidence pages, same-database jobs, captured attempt/lease, identical retry deduplication and mismatched-payload rejection | Real process death, worker cancellation and producer execution after merge. |
| Atomic revision checks and late evidence | SQLite exclusive transactions, snapshot CAS, stale results historical and compatible results awaiting review | Integrated concurrent phone capture/editor/analysis runs. |
| Partial writes, low space, missing original | Real store harness with simulated files and actual Node SQLite; journal retry, rollback and no-overwrite checks | Physical low-storage/process-death/missing-media tests. |
| Registered asset cleanup and deletion separation | Registered completed assets, existing deletion tombstones/cancellation/cleanup, source preservation on timeline commit | Permission failures/gallery/shared-file cases on device. |
| Saving/Analyzing/Ready/partial/failed/cancelled, Open editor/Retry | Reusable controlled component and Projects integration, no percentages, queued jobs described as waiting | B/capture integration, large text, screen reader and Android back/cancel on integrated iQOO. |
| Bounded long-session evidence/history | Awaited bounded producer pages, per-command archive and chunked large audit records, paginated reads | Producer backpressure/full-transcript sink integration and measured long-session memory on device. |

## Local verification

Exact command outputs are stored under `docs/validation/evidence/t15-durability/`.
The initial focused run passed 33 schema/persistence/pickup/lifecycle checks and TypeScript checking.
A separate real-store harness imports `store.ts`, uses actual Node SQLite and a simulated Expo filesystem; its capture/save/recovery/pickup/deletion flow passed.
The simulated filesystem and injected `SQLITE_FULL` checks are not physical-device evidence.
Final broad command counts and exit statuses are recorded below after verification finishes.

## Producer and consumer evidence collected read-only

| Lane | Inspected published revision/status | Reported evidence and limitations |
| --- | --- | --- |
| Speech #70 | OPEN draft, head `f6062f009742611efb0e6c6ac62f727234d15c17` | Current PR body reports 475 JS tests, typecheck, 19 samples, caption/coverage replay, 12 speech and 11 T1 evaluator checks, Android JS export. Synthetic matcher: 0 false matches and 1 missed clause-reordered paraphrase in 12 cases; point baseline overselects 3/8 lines. These are producer reports, not A reruns or semantic/device acceptance. |
| Capture #71 | OPEN draft, head `93c4c850cc494c63ba86ecb3217a6f6551237fe9` | Reports synthetic no-pre-tap jobs, unknown-only face-engine gaze, JS/sample/export/JVM checks and historical native build. Counts differ across historical snapshots (365 then 462 after refresh). Complete gaze persistence is explicitly pending. No physical cadence/A/V/NPU/human acceptance. |
| Proposal #72 | OPEN draft, head `3771653e748eeb99cf66761f46c0adc78f86c5ae` | Design-only proposed schema/handoff and pending matrix. No executable foundation or agreement was imported. |
| B #67 | Published `904ccc5`, branch `feat/t15-editable-timeline` | Runtime clip/history/resolver request read; A accepted fields in #65 comment. B's implementation and integration evidence are still to be collected. |
| C #68 | Branch `feat/t15-independent-captions` | Published request for independent preferences and merged-only integration read. C's final implementation/output evidence is still to be collected. |

Producer evidence lives in their PR branches at `docs/handoffs/t15-session-1-acceptance.md`, `docs/handoffs/evidence/t15-session-1/`, `docs/handoffs/t15-session-2-verification.md`, and `docs/handoffs/evidence/t15-session-2/`.
Those paths are references to unmerged producer branches, not files copied into this implementation.
Both producers' older “#58 open” claims are historical; current main includes #58.
Their old web WASM blocker is also historical: current main already contains the Metro WASM/header fix, which A preserves.
No reviews or inline review comments on #70/#71/#72 supplied human sign-off at inspection.

## Release gates explicitly pending

Both full Home/Script or Assisted → Record → Stop/durable save → analysis → editor → reopen → standalone export paths on integrated iQOO remain pending.
The One Take paraphrase example, changed-fact/name/number/negation negatives and recoverable skip/reanchor require real human reads and semantic evaluation.
A,C plus pickup B, reverse-order pickups, shared whole utterances, manual edit conflicts, undo/override and reopened caption alignment require integrated producer/B/C evidence.
Five back-to-back clean/flub/repeat/partial/noisy sessions, real STT accuracy and timings are pending.
Trim/split/exclude/reorder/undo/redo/reopen/export, all-clips-excluded, source hashes, actual audio order and all four caption combinations are pending.
Approximate gaze cadence/provenance, no-pre-tap suggestion jobs, thermal/fallback/NPU behavior and recording impact require actual device measurements.
Permission, interruption, process death, low storage, missing media, gallery/share and recovery UX remain physical-device checks.
Ten-clap and long-recording A/V evidence remain pending.
Large text, screen reader, accessible controls and Android back/cancel remain pending.
Named non-author reviewers and both peer reviews of shared timing/data-loss interfaces remain pending.
No device was installed, launched, cleared, recorded or otherwise mutated by A.
Exclusive device access must be coordinated before those checks.

## Integration responsibilities

Exact APIs, units, duplicate/revision rules, examples and owner-authored requests are in `docs/handoffs/t15-durability.md`.
B integrates editor commands/history and the lifecycle component; C integrates caption settings/mapping/export against the chosen sequence.
Capture integrates full observation delivery and Stop metadata using the existing journal; speech supplies final producer envelopes after merge.
The foundation has no imported final-analysis runner while #70 is open; queued Retry is durable work awaiting the consumer, not a claim analysis ran.
Expo SDK 57 native exclusive transactions are unavailable on web, so native durability tests do not establish web persistence support.
A published handoff is not agreement, a passing fixture is not a physical-device check, and a draft PR is not integrated V1.
