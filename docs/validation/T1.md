# Tier 1 integrated verification report

Status: NOT ACCEPTED; all Tier 1 release gates remain false.
Date: 2026-09-13.
Lane: `feat/t1-review-wrap-reframe`, draft [PR #58](https://github.com/AlwinSunil/one-take-expo/pull/58).
Verification baseline: `552fd00`.
Initial additive contract delivery is `46d5bc0`; core native integration is `414d748`.
Final cleanup source checkpoint is `e782e18`; its full suite has 420 passing tests.
Merged Tier 0 main `11f4455` was incorporated at `3c36d41`; merged producer main `15d80d6` (#59/#60) was incorporated at `3536a88`.
No unmerged Tier 0 or Tier 1 producer work was merged, rewritten or cherry-picked.
This report coordinates #40 but does not satisfy the two other contributors' independent reproductions.
The exact Expo SDK 57 documentation and repository acceptance/research were read before code changes.
T1.5 #62 and its expanded editor/important-point requirements remain outside this lane's scope.

## Acceptance matrix for every T1 child

| Issue | Lane | Evidence in this branch | Acceptance still required |
| --- | --- | --- | --- |
| #22 | 1 | Merged producer reasons, scratch and take contracts; consumer reason/scratch contract and supplied reason UI | Live capture identity/producer mapping, held-out command precision and named Alwin review |
| #23 | 1 | Merged must-say producer and conservative wrap consumer display; pending/stale/missing evidence fails closed and caption corrections cannot satisfy wording | Live captured revision/wording publication, capture-side wrap wiring, held-out false-confidence results and named Alwin review |
| #24 | 1 | Merged cleanup producer preserves raw speech and reversible decisions; gated cleanup review accepts verified removals only within an existing prepared sequence | Live cleanup producer wiring, quiet/noisy/stutter/repeat/filler listening, independently verified boundaries and no automatic mid-sentence splice |
| #29 | 1 | Merged live transcript producer and default-off common gate preserved | Live route consumption, long/no-speech/delayed capture and large-text device review; named Alwin review |
| #28 | 2 | Merged coach implementation present; optional coach gate provided | Unseen good/bad talking-head/vlog/product scenes, intentional-framing silence, between-line timing, real processor/fallback; named Melvin review |
| #35 | 2 | Structural provider adapter preserves identities, units, frame and provenance; missing proof falls back | Producer protected tracks/union/boundary/caption-area agreement and integration; multi-subject/device performance; named Melvin review |
| #30 | 3 | Supplied reasons/footage, take choice, legacy reversible cuts, word save/reset/undo; synthetic Android caption correction/export flow observed | Integrated #22/#24 producer outputs, full reread/cleanup listening, long transcript/accessibility matrix and named Sabari review; the atomic draft seam has pure round-trip tests only and no DB/device claim |
| #31 | 3 | Per-line coverage/must-say/take count/timing/evidence; separate manual actions; durable context-bound Wrap anyway; baseline direct pickup route | #23 producer and capture-side wrap wiring; producer-only must-say pickup route; full capture-to-wrap duration/device matrix; named Sabari review |
| #38 | 3 | Static crop planner, common native preview/export composition, off/reset and fail-closed fallbacks; four Android synthetic renders rerun after caption-area safety fix | Stronger #35 interval/geometry/track proof, full preview/render parity and crowded caption layout inspection on real portrait/moving/product scenes; named Sabari review |
| #40 | 3 coordination | Automated merged-Tier-0 checks, native build/APK verification, synthetic device review/render evidence and this report at `552fd00` | Full #34 regression, all feature gates, held-out speech/command/must-say, stronger framing proof, both independent reproductions and named human UX review |

Every issue remains partial or pending acceptance.
Named reviewers remain those in the issues even though implementation lane ownership was reassigned for this work.
A separate automated code review identified stale-save, unavailable-media, action-only-line and pickup-caption defects; fixes and regressions were added.
That review is not named human or cross-family evidence.

## Exact automated checks

Run from the lane worktree with dependencies installed using `npm ci --ignore-scripts --no-audit --no-fund`.
No `.env` was read; Expo commands use `EXPO_NO_DOTENV=1`.

| Command | Result |
| --- | --- |
| `npm run typecheck` | Passed |
| `npm test` | 420 passed, 0 failed, including 11 cleanup review cases |
| `npm run samples` | Deterministic caption replay passed |
| `npm run test:samples` | 19/19 shared fixture checks passed |
| `python3 tests/speech-evaluation.test.py` | 12 passed |
| `python3 tests/t1-speech-evaluation.test.py` | 11 passed |
| `python3 tools/speech-fixtures/run_synthetic.py` | 16 synthetic rows; zero eligible held-out human clean/flub rows |
| `git diff --check` | Passed |
| `:one-take-captions:testDebugUnitTest` | 15 passed, 0 failed |
| `:one-take-media:testDebugUnitTest` | 12 passed, 0 failed, including six timeline/crop tests |
| Android `:app:assembleDebug` | Passed, arm64-v8a |
| `python3 tools/verify_moonshine_apk.py android/app/build/outputs/apk/debug/app-debug.apk` | 3 native + 8 Tiny + 8 Small packaged SHA-256 values matched |
| Host framing renderer | Nine deterministic decisions passed, including caption/hand conflict fallback |

Cleanup review was checked with deterministic producer plans only.
Its UI listening flow, rendered splice quality and human boundary acceptance remain pending.

Node reports the existing MODULE_TYPELESS_PACKAGE_JSON performance warning.
Gradle reports deprecations and React Native C++ deprecation warnings; neither failed the build.
No lint pass is claimed: no repository ESLint setup was available and the attempted auto-install was cancelled without retaining dependency changes.
Final Android Hermes export passed after cleanup UI wiring: `EXPO_NO_DOTENV=1 npx expo export --platform android --output-dir /tmp/t1-close-android-export`.
Local build logs are `/tmp/t1-close-bundle.txt` and `/tmp/t1-final-native-build.txt`; these temporary files are not published evidence.

Native build command:

```sh
EXPO_NO_DOTENV=1 JAVA_HOME='/Applications/Android Studio.app/Contents/jbr/Contents/Home' ANDROID_HOME=/Users/sabari/Library/Android/sdk ./android/gradlew -p android :app:assembleDebug :one-take-captions:testDebugUnitTest :one-take-media:testDebugUnitTest -PreactNativeArchitectures=arm64-v8a --console=plain
python3 tools/verify_moonshine_apk.py android/app/build/outputs/apk/debug/app-debug.apk
shasum -a 256 android/app/build/outputs/apk/debug/app-debug.apk
```

The pinned native package was staged through `tools/prepare_moonshine.py --native-dir /tmp/t1-moonshine-native/arm64-v8a --with-small` before building.
Use the repository's documented model preparation rather than bypassing its artifact guard.
The host renderer uses `python3 tools/render-t1-framing.py`; provide FFmpeg using its documented option or the isolated `imageio-ffmpeg` executable.
See `tools/render-t1-framing.py --help` for regeneration and output options.

## Physical Android evidence

Device: iQOO I2501, Android 16 / API 36, USB arm64-v8a.
App: `com.anonymous.onetake`, version 1.0.0/code 1, debuggable development build, Expo 57.0.22 / React Native 0.86.3 / React 19.2.3.
Installed APK SHA-256: `95c446b385a886a48700d10347ae2d28db8c2174c73a3e5d3983f39bbf7ca1f7`.
Native sources match the core framing implementation published at `414d748`; subsequent review/UI safety fixes were loaded through Metro on this binary.
No recognition or vision model was invoked by these fixture checks.
Packaged model configuration is Moonshine Tiny Streaming plus Small, custom Moonshine v0.1.5 / ONNX Runtime 1.28.0; packaging is not inference evidence.
The phone used for the synthetic flow has been released; no current device reservation or live capture acceptance is claimed.
The actual export log names `c2.qti.avc.encoder`; decoding named `c2.qti.avc.decoder` and `c2.android.aac.decoder`.
See [codec log](evidence/t1-session-3/native/codec.txt).
No NPU delegation, measured GPU processor, human speech accuracy or A/V synchronization result is claimed.

Observed native flow:

1. Open `onetake://t1-review-fixtures` and explicitly enable Tier 1 test.
2. Missing-media fixture shows editable captions, disabled footage, uncovered spoken line, pending must-say and separate required action.
3. Tap a caption word, enter a replacement and explicitly Save word; serialized and SQLite reopen preserve the display correction.
4. Wrap anyway records acknowledgement and retains pending/missing flags; context invalidation is covered by behavior tests.
5. Create synthetic video project, open actual editor, enable development test, correct “battle” to “bottle”, save, force-stop and reopen the project.
6. SQLite readback showed `text: 'Our battle stays cold.'` and `manualCorrection: 'Our bottle stays cold.'` together.
7. Export the prepared 2–5 second sequence through the real Export video control, explicitly accepting the current fixture caption timing estimate.
8. Job completed at 100%; the retrieved MP4 visibly contains the corrected caption and keeps the geometric original subject.

[Corrected rendered frame](evidence/t1-session-3/native/editor-caption-corrected.png) and [actual MP4](evidence/t1-session-3/native/editor-caption-corrected.mp4) are synthetic media evidence from the editor flow.
[SQLite fixture screenshot](evidence/t1-session-3/caption-sqlite.png) and [Wrap anyway screenshot](evidence/t1-session-3/wrap-anyway.png) record earlier native fixture observations, not final producer acceptance.
The new atomic script draft snapshot is covered by pure serializer/reopen/failure tests only; no SQLite or device evidence is attributed to that seam.
No real camera recording or user project was deleted; test media was created in separate synthetic projects.
Caption keyboard inspection exposed obscured inline controls, so word editing now uses a focused dark modal with keyboard avoidance and explicit Save/Cancel.
[Keyboard screenshot](evidence/t1-session-3/caption-keyboard.png) shows the input and Save button above the Android keyboard; saving through this final modal was observed.
The phone was force-stopped and returned home, and both temporary adb reverse entries were removed after testing.
Small-screen/large-system-text and independent accessibility review remain pending even after this fix.

## Framing reproduction and limits

Open the same development fixture route and choose the Native framing fixture panel.
It requires the native `supportsFraming` capability.
Use Preview selected, Use original frame, Export selected or Run four native exports.
The request carries the same segment crop for NativeCutPreview and Media3 export.
The panel reports exact case IDs, fixture paths, source identities, decisions, native results and output URIs, and saves `t1-native-render-report` to a dedicated SQLite setting.
No gallery or share action is invoked.

The synthetic portrait, moving and product assets contain geometric subjects and silent AAC.
Four Android synthetic exports completed: portrait 3 seconds, moving fallback 3 seconds, missing-track fallback 2 seconds and product 4 seconds.
Encoded video is H.264 with rotation metadata displayed as portrait 720×1280; audio is AAC 48 kHz stereo.
Container duration is approximately 3.01 / 3.01 / 2.00 / 4.02 seconds, which is not a clap or lip-sync measurement.
[Native report](evidence/t1-session-3/native/render-report.json) and [media info](evidence/t1-session-3/native/media-info.json) retain the exact recorded outputs.

The first product render retained the source hands but placed a caption over a hand.
It is failure evidence, not accepted output.
The planner now rejects overlap between the reserved caption area and protected face/product/hand regions and uses the original frame.
All four native cases were rerun after that fix and completed; the final product frame keeps both hands above the caption.
The initial report and occluded product sample remain prefixed `initial-` as failure evidence.
[Final product fallback](evidence/t1-session-3/native/product-keeps-product-and-hand.png) and [portrait native preview](evidence/t1-session-3/native/preview-portrait.png) were visually inspected.
The portrait preview reported a 119 ms first frame in this single synthetic run; this is not a performance acceptance benchmark.
The host report labels the old product-reframed artifact as superseded.
The existing fixed native lower-third overlay can still overlap objects in a crowded original frame; this remains a release acceptance limitation requiring real-scene inspection and a separate safe placement decision.
No crop is continuously animated; missing, moving, uncertain, stale, ambiguous or unsafe suggestions keep the original frame.
The producer adapter does not guess missing track/coverage semantics.

## Browser check

`EXPO_NO_DOTENV=1 npx expo start --web --port 8093` served `/t1-review-fixtures` in Chrome at 390×844.
The explicit test gate, missing-media and pending-wrap states were inspected; [small viewport screenshot](evidence/t1-session-3/browser-small.png) is retained.
The audit found and fixed Metro WASM assets/COOP/COEP setup, SQLite static-render incompatibility and NativeWind dark-mode configuration.
Web uses single-page output to preserve SQLite behavior.
No production website was published.
This browser check does not verify Android media behavior or a full browser correction/export workflow.

## Full Tier 0 regression required by #34

Tier 0 implementation PRs are merged, but their human/device acceptance is not inferred from this lane's automated checks.
Run the full protocol in the merged `docs/validation/T0.md` and #34 on the eventual integrated release candidate and record its SHA.
The following remain pending in this T1 report:

- Five consecutive three-line captures with deliberate flubs, between-line prompts or honest delay, clean-line retention, required actions and immediate review.
- Record/draft/project/trim/original recovery, no-speech/pending/missing media, targeted pickups and return to the same project.
- Captioned export progress/cancel/retry/interruption, gallery/open/share, deletion recovery, process crash/restart, permission denial and low storage.
- Ten clap recordings with at most one-frame spread, two-minute drift measurement, human listening and lip-sync review.
- Warm-phone sessions, actual processor/fallback, memory/heat/battery, hardware remote and audio-route changes.
- Basic coaching on staged problems and intentionally good framing, quiet during speech.
- Required held-out speech sample counts, including Tier 0 clean/flub and Tier 1 command/must-say precision, disjoint from tuning samples.
- AlwinSunil and Melvin0070 independently reproduce the integrated path and review the UX recording, small screens, large text, back/cancel and failure recovery.

## Remaining integration dependencies

[Session 3 handoff](../handoffs/t1-session-3.md) contains exact types, units, identity/lifecycle rules, examples and requested owner-authored changes.
PR #59 supplies speech reasons, scratch/must-say/cleanup producers; PR #60 supplies capture/coaching/framing producers.
Both are now merged and included at `3536a88`.
The actual framing producer fixtures are invoked by `t1-framing-integrated.ts`; absent stronger interval/geometry/caption proof still yields original-frame fallback.
Merged speech components still need owner-authored live capture/script wiring and durable captured revision/take identities; no live semantics were invented in the consumer.
The common gate remains default-off until each feature's acceptance checks and the full Tier 0/#34 regression pass.
The framing integration remains fixture-backed until protected tracks, interval unions, boundary provenance and caption-safe placement are proven by the producer on real scenes.
Agree the remaining adapters and rerun affected checks.
Capture still needs the report at its wrap entry and support for producer-only must-say pickups whose baseline coverage is already satisfied.
Session 2 must provide protected geometry, interval-union/boundary proof and safe-caption metadata before its framing suggestions can be applied.
Issue #53's splice safety concern and the full #34 regression remain release concerns.
Named human review, held-out speech/command/must-say accuracy and independent reproductions remain pending.
Do not enable any Tier 1 feature or mark #40 complete from local tests or synthetic outputs.
